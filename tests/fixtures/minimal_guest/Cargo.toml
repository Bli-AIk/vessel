[package]
name = "cauld_ron_test_guest"
version = "0.1.0"
edition = "2024"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
cauld-ron = { path = "../../..", default-features = false, features = ["guest"] }
